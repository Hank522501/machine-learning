# 模型评估

如果一个模型预测效果并不好，我们一般通过什么方式改进呢？现在用一个线性回归问题来举例。

![debug linear model](https://i.mji.rip/2025/12/18/0200d10f7be65ac91b9f0806fd8c8c9e.png)

对于上述线性回归问题，我们一般通过下列几种方式调整，但对于使用哪种方式能够将模型改进到预期效果，我们目前也只能逐个尝试。

* **增加训练数据**
* **减少特征**
* **增加特征**
* **增加特征多项式** (x_1^2, x_2^2, x_1x_2, \text{etc})
* **降低** \lambda 数值
* **增加** \lambda 数值

有些模型预测效果好有些预测效果差，对于预测效果差的模型，用曾经学习过的知识表述就是有些模型过拟合，有些模型欠拟合，总之就是
模型没有拟合。对于过拟合和欠拟合我们只有一个定性的表述，由人通过模型函数线去判断，而没有定性的方式去得出结论，这样对于模型
的改进是不利的，所以我们需要一些可以定量判断一个模型是否理想、效果是否好的方法。

## 测试集

还是回到线性回归问题预测房价的示例，当我们使用了较多的特征和多次项式时，整个模型看起来是过拟合的，不过我们需要更加定量和科
学系统的办法进行判定。

![linear predict house price](https://i.mji.rip/2025/12/18/4cdfcba411ac7b18c1b283c7981a3573.png)

下列是训练预测房价模型的数据，我们现在可以在拿到数据集时将整个数据集按八比二或七比三的比例，划分为训练集和测试集。顾名思义，
训练集只用于训练模型，而测试集不参与模型训练，只用于对训练完的模型的进行检测。

![house-price data](https://i.mji.rip/2025/12/18/e0ba53688ae2c869c9a54b16d6548dff.png)

在划分好训练集和测试集后，我们可以再次利用线性回归的正则化数学表达式表述训练效果和测试效果，如下图。训练集和测试集的代价
函数不会使用正则化，因为我们只需要评估模型与训练集、测试集的代价函数结果就能够评价训练得到的模型效果如何。只有当测试集和
训练集的代价函数计算结果都比较好时，这个模型才是符合预期的。如果两个代价函数结果中训练集的结果不好，表示模型欠拟合；如果
测试集的结果不好而训练集的结果好，则表示模型过拟合。

![linear training and testing cost function](https://i.mji.rip/2025/12/18/2ab991919cd11c42dbdefea259408440.png)

上述对模型是否理想的判断方法对于逻辑回归问题同样有效，如下图。因为逻辑回归问题主要是分类问题，也可以直接通过预测结果与真实
结果匹配，然后计算匹配率的方式判断。例如一个二元分类问题，可以将训练集和测试集提供给模型进行预测，得到的预测结果 y 值与实际
的 y 值结果计算匹配的比率，然后对模型进行评价。

![logistic training and testing cost function](https://i.mji.rip/2025/12/18/cd3e873502f8d76423a97e0702336c55.png)

## 验证集

继续上面预测房价的线性回归问题，此时我们需要决定模型使用几阶的多项式，如下图。如果我们只通过训练集进行训练，再经过测试集的验
证进行选择，我们往往不会得到一个合适的模型。因为模型的阶数应该是训练模型拟合需要决定的，如果测试集参与了这个过程，最终的模型
对于测试集的验证是偏乐观的，无法验证出一个更优的模型。

![linear multi order](https://i.mji.rip/2025/12/18/7ce4fbd4012ad7baaf230536b3c34489.png)

因为一个模型不可能只经过训练集的训练就能达到理想效果，而测试集又不能参与模型训练调优的过程。如果使用测试集参与模型训练与调优
很可能会导致模型拟合测试集数据，最后评测模型的结果过于乐观，很有可能模型并未达到预期。所以，我们需要将整体数据集划分为训练集、
验证集和测试集，分别按六比二比二的比例划分。其他验证集可以叫做交叉验证集或开发集，它主要用于帮助通过训练集训练的模型进行模拟
测试，然后根据测试集结果对模型进行调优。

与之前通过训练集和测试集对模型进行判断一样，当引入验证集后也是通过验证集的代价函数计算结果共同评价一个模型的优劣，如下图。

![training testing and validation cost function](https://i.mji.rip/2025/12/18/abfb1b6ed62c554556d10bcf44ddf147.png)

## 方差与偏差

在模型的评价中，除了欠拟合和过拟合外，我们还会通过方差和偏差对模型进行评判，例如下面的三个模型函数线。

![bias and variance](https://i.mji.rip/2025/12/18/7f5887035168b2f3e69661499a5eaf53.png)

上述三个模型，我们可以看出左侧的模型属于欠拟合，右侧的模型为过拟合，而中间的模型属于拟合。从方差和偏差的角度来说，欠拟合的模型
是高偏差的，而过拟合的模型是高方差的。高偏差和高方差也可以通过前面我们学习的训练集和验证集表述，对于高偏差的模型来说，训练集的
代价函数结果较高，同时验证集的代价函数结果也较高。对于高方差的模型来说，训练集的代价函数结果较低，但验证集的代价函数结果会比较
高。对于拟合的模型来说，测试集的代价函数结果和验证集的代价函数结果都不会太高。

因为上述示例是一个线性回归模型，它们分别表示一次多项式、二次多项式和四次及更高次的多项式。我们可以通过一个横坐标为次幂数，纵坐
标为代价函数结果的坐标系来观察和理解方差和偏差，如下图。

![degree bias and variance](https://i.mji.rip/2025/12/18/645fb62d0cecaa7ce83875814800352f.png)

最后，关于方差、偏差与训练集和验证集的代价函数结果关系可以总结如下图。

![summary bias and variance](https://i.mji.rip/2025/12/18/c91b5dea0709ca0cc9f6c95debbf0e1f.png)

## 正则化

在上面线性回归的示例中，我们并没有对模型使用正则化，如果我们对模型进行正则化处理后，模型函数将如同下图所示。

![regularization bias and variance](https://i.mji.rip/2025/12/18/63d9c1d137d94afa9864ed2299ab27a7.png)

正如上图所示，当 lambda 很大时，所有的 w 值都会趋近于零，导致模型最后呈现一条横线。此时模型是欠拟合的，也就是高偏差的，训练集数
据代价函数和验证集数据代价函数结果都是较大的。当 lambda 很小时，所有的 w 值都会尽量保持使模型贴近训练集数据，所以模型是过拟合的，
也就是高方差的，训练集数据代价函数结果较低，但验证集数据代价函数结果较大。所以，通过不断尝试不同的正则化参数 lambda，我们能够通
过训练集、验证集的方式找到使模型拟合的参数。

我们还是通过一个类似线性回归模型幂次与代价函数差值的坐标系，展示 lambda 与训练集数据和验证集数据代价函数的关系，如下图。它就如同
幂次与代价函数绘制的坐标系的镜像函数线一样。

![regularization bias and variance](https://i.mji.rip/2025/12/18/c36c12661a39e8f2d563490d2b2b6c98.png)

## 建立基准

在前面知识的学习中，当验证集误差与训练集误差之间的差值较大时，我们可以明确判断此模型是高方差。但是对于高偏差的模型，我们并没有相应
的明确比对，只能说训练集误差较大时。然而大小是一个比较级的事情，所以只有当训练集误差与某个数值比较时，才能评判训练集误差是否较大，
那么我们就需要引入一个基准值，作为与训练集误差进行比较的数值，依此判断模型是否高偏差。

现在，我们举一个主意识别的例子。如今很多手机上都已经安装了语音助手，例如 Siri、小爱同学等，人们有时候搜索并不会直接输入文字，而是
通过说话输入语音的方式，让语音助手识别语音指令，并帮助人们完成搜索工作。假如我们现在也有一下类似的语音识别模型，并且计算得到它的训
练集误差为 10.8%，我们能否判断该模型是一个高偏差的模型呢？目前不行，我们需要引入一个基准值与训练集误差进行比较，才能进行模型是否高
偏差的判断。这个示例中，我们可以将人类识别语音的误差作为基准，因为语音中有时候是存在噪音、口音等干扰的，所以人类也无法完美地识别所
有语音。假设人类识别语音的误差在 10.4%，与训练集误差比较起来，这个模型的训练集误差与人类识别语音的误差区别不大，所以该模型并不是高
偏差的。

所以，如果要判断一个模型是否高偏差，我们需要引入相应的基准值。通常，我们可以将人类的表现水平、已经存在的同领域模型或根据经验进行估
计这三种方式得到的误差结果作为基准值，与训练集误差比较，用于评价一个模型是否高偏差。此时，我们总结一下，当训练集误差与基准值相差不
大，而训练集误差与验证集误差相差较大时，我们认为此模型是高方差模型；当训练集误差与基准值相差较大时，我们认为此模型是高偏差模型；只
有当训练集误差与基准值和验证集误差相差都不大时，这个模型才是一个符合预期的理想模型。

## 学习曲线

例如，现在我们有如下二次多项式。

![learning curve function example](https://i.mji.rip/2025/12/19/077d6a7419038fc400905c8e2ffaf7f0.png)

随着训练集数据规模的增加，训练集误差会逐渐上升最后趋于不变，验证集误差会逐渐降低最后趋于不变，但验证集误差始终都会比训练集误差高一
些，如下图。当训练数据规模越来越大时，模型要拟合所有的数据会变得越来越难，所以训练集误差会逐渐变大，当达到某个节点后，训练数据规模
不再影响训练集误差；对于验证集误差也是类似的道理，当训练数据规模较小时，模型比较简单，对于验证集拟合不太好，造成验证集误差较大，随
着训练数据规模增大后，模型更加平滑和灵活，能够更好地适配验证集数据，所以验证集误差逐渐下降，当达到某个节点后，验证集误差趋于平缓。

![training and validation learning curve](https://i.mji.rip/2025/12/19/b6d1ff3916eed55896a6315a48bd5505.png)

接下来我们观察高偏差情况下的学习曲线，对于一个一次一项的学习算法来说，高偏差状态下的学习曲线与前面一个例子的差不多，如下图。也是训
练集误差随着训练集数据规模增大逐渐增长，然后在某个节点趋于平缓；验证集误差随着训练集数据规模增加逐渐降低，然后在某个节点趋于平缓。
有所不同的是，在学习曲线的坐标系中绘制基准线时，基准线的误差值会远远小于训练集的误差。同时，我们无法通过增加训练数据规模的增加修复
高偏差问题，通过观察学习曲线，训练集误差是随着训练数据规模的增长而增长，然后在某个节点趋于平稳，并不会随着训练数据规模的增长而减小，
所以无法通过增加训练数据规模使高偏差的问题得到解决。

![high bias learning curve](https://i.mji.rip/2025/12/19/8d27617c84f511e42efa9848b070fdb6.png)

对于高方差的算法略有不同，如下图。在高方差的算法的学习曲线中，训练集误差会随着训练数据规模的增长而增长，然后增长速度逐渐放缓；验证集
误差会随着训练集数据规模增长而降低，然后降低的速度逐渐放缓。在高方差的情况下，训练集误差可能低于基准线也可能高于基准线，但总之是优于
基准线或与基准线接近的，训练集误差与验证集误差的差值就会比较大。不过，我们可以通过增加训练集数据规模的方式使训练集误差与验证集误差趋
近，以此解决高方差问题。

![high variance learning curve](https://i.mji.rip/2025/12/19/571bb326c9a8119fe328acfd59257a79.png)

## 优化模型

经过前面几个小节知识的学习，我们系统地掌握了评判机器学习模型优劣的方法，同时从各个角度深入理解了这些评判办法。那么，让我们回到如果机
器学习算法不够理想时要如何调整的问题，具体问题如下图。

现在我们需要思考，当我们发现一个线性回归问题的模型不够理想时，我们应该可以采取哪些办法优化，同时这些优化方法解决的是什么问题，可以采取
的优化方法如下。

* **增加训练数据**
* **减少特征**
* **增加特征**
* **增加特征多项式** (x_1^2, x_2^2, x_1x_2, \text{etc})
* **降低** \lambda 数值
* **增加** \lambda 数值

我们解析一下上面的各个优化方法分别在解决什么问题。第一个优化方法，我们已经在学习曲线的章节实际观察过，当增加训练集数据规模时，训练集误
差和验证集误差会逐渐趋近，学习算法的高方差问题在得到改善；第二个优化方法，当减少特征时，学习算法就会得到简化，会降低它的平滑和灵活性，
降低学习算法的过拟合，也就是改善学习算法的高方差问题；第三个优化方法，当增加特征时，学习算法的计算项会增加，会增加它的平滑度和灵活性，
增加学习算法的拟合程度，也就是改善学习算法的高偏差问题；第四个优化方法，当增加多项式时，其实和增加特征量是相似的优化方法，同样是改善机
器学习算法的高偏差问题；第五个优化方法，当降低 lambda 的值时，就降低了正则化对各计算项的处罚，也就是机器学习算法的计算项会增加，所以它
也是改善机器学习算法的高偏差问题；第六个优化方法，增加 lambda 的值，其实也就是增加了正则化对各个计算项的惩罚，降低了机器学习算法的计算
项，也就是改善了机器学习算法的高方差问题。

虽然优化机器学习模型的方法很多，但总结起来就是如果优化方法最终会简化机器学习算法（如减少特征、增加训练数据、增加 lambda 值）时，主要改善的
是算法的高方差问题，如果优化方法最终会丰富机器学习算法或复杂化机器学习算法（如增加特征、增加特征多项式、降低 lambda 值）时，主要改善的是算
法的高偏差问题。

## 关于神经网络

在传统的机器学习算法中，工程师们总是需要考虑偏差与方差的平衡点，既不能使算法是高偏差的，也不能使算法是高方差的，如下图。

![trade off bias and vanriance](https://i.mji.rip/2025/12/19/52fa4fdc0fd44de69e6c60fab2a37134.png)

如今，神经网络模型带来了一些新的视角，大型神经网络模型在小规模或中午规模的训练数据集上进行训练时，是一个低偏差模型。所以，只要神经网络做得
足够大，它总能拟合训练集。于是，我们可以根据需要分别降低偏差或方差，而不需要像传统的机器学习一样，在高偏差和高方差中寻找平衡。

如果要构建一个低偏差且低方差的神经网络模型，可以按照下图中的步骤训练。首先计算神经网络模型的训练集误差，如果误差较高（也就是高偏差）时，就
使用一个更大的神经网络模型，也就是有更多隐藏层或隐藏层有更多神经元的神经网络模型，直到神经网络模型能够与训练集拟合，训练集误差与基准值相媲
美。然后继续进入下一步，再计算神经网络模型的验证集误差，如果验证集误差远远高于训练集误差，那么该模型存在高方差问题，我们可以加入更多的训练
数据，并回到前一步，继续训练神经网络模型。一直持续这个循环，当神经网络模型既不是高偏差也不是高方差时，我们就得到了表现良好的神经网络模型。

![nerual network low bias and low variance](https://i.mji.rip/2025/12/19/559613473e90f7b90034706e16538311.png)

到了这里，其实大家还会存在一个疑问，如果神经网络模型规模增长，是否会增加过拟合的风险？其实不然，只要对神经网络模型进行合适的正则化，即使神经
网络模型规模增长，它的表现与小型的神经网络模型相比也不会有太大变化，甚至可能比小型的神经网络模型表现更好。不过大型的神经网络模型还是存在一些
困扰，比如它训练时需要更多的资源，也会在训练上有更多的耗时。
