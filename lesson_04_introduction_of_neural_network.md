# 神经元与大脑

## 神经网络的发展

神经网络算法在起初时，是为了尝试使用计算机模拟人脑。在 19 世纪 80 年代到 90 年代之间开始使用神经网络算法，当时人们主要使用神经网络进行数字识别，例如邮件的
编码、手写支票的数字等。而后在 90 年代后遇冷，在 2005 年左右再次开始崛起。

神经网络开始展现重要作用应该是在语音识别领域，而后在图像识别领域发挥作用，使计算机视觉领域更具想象力。随后神经网络在自然语言（NLP）领域也开始规模应用，并直
到如今在各个方面在生活的方方面面发挥其作用，例如气候分析，医学影像分析，内容推荐等。

## 神经网络的生物学原理

在生物学上我们可以认为神经元由神经核（Nucleus）、树突（Dendrite）和轴突（Axon）组成，大脑中的电信号由树突传入，经过神经核的处理，再通过轴突向另一个神经元传递。

在神经网络算法中也是类似生物学中神经元的结构，由输入经过神经元的计算再到输出，通常我们会在计算机中同时模拟多个神经元，最终形成如下图的结构。

![](https://i1.hdslb.com/bfs/note/e6bffb331d2f3770ef2dbeadffc577c6de97e078.jpg@690w_!web-note.avif)

## 神经网络为何崛起

回忆之前我们学习的线性回归和逻辑回归模型，在提供数据量增加的同时，并不会显著提升模型算法的性能。而如今，社会的信息化程度极高，大量工作和人类活动已经电子化、数字，
这些数据如果只是提供给传统 AI 算法显然没有得到最大的利用。但神经网络算法呈现出不一样的效果，随着神经网络模型的规模（神经元数量）越大时，训练数据集越数量的增加会
使模型的性能得到显著提升，所以这就是神经网络如今大行其道的原因。

![](https://i1.hdslb.com/bfs/note/b28a471d706c55d0129d47c1034fefc32a301398.jpg@690w_!web-note.avif)

# 案例-需求预测

## 预测 T 恤能否畅销

现在我们从一些实际的案例出发，来认识神经网络模型。现实情况中，我现在打算售卖一种衣服，但我想提前预估一下这种衣服是否能够畅销，这样可以有利于我对进货、资金等的管理，
以保证这场生意我能够更大机率的盈利。

说到这里，我们脑海中应该会快速浮现之前学习的逻辑回归模型，因为这个示例中我们只需要根据特征数据分析衣服是否畅销（是或否），非常符合分类问题的特点。

不过我们肯定不止是让你回顾旧知识，这样的示例同样可以使用神经网络模型进行结果预测。例如，输入的是价格，经过神经元计算产生畅销概率作为输出。

![](https://i1.hdslb.com/bfs/note/4f8c8fc9c63ab870f04f2e4c115c145875b3d911.jpg@690w_!web-note.avif)

## 构建神经网络进行预测

在实际情况中，一种衣服是否能够畅销往往和许多因素有关。衣服这件商品本身有一些基础属性（也可以说是特征值），例如价格、运输成本、材质、营销等。我们当然可以直接根据衣服的
这些基础特征直接预测畅销概率，不过在这些基础特征之上，一件衣服是否畅销可能由一些综合性因素决定，例如可负担性、人群关注度、性价比等。所以我们需要根据基础特征值预测这些
综合因素，最后由综合因素的预测结果产生衣服畅销的概率。

![](https://i1.hdslb.com/bfs/note/f1cba297d260bfd7dd78c33f63bfbb00fd50ada2.jpg@690w_!web-note.avif)

在上述示例构建的神经网络中，最左侧为输入层（Input Layer），最右侧为输出层（Output Layer），而中间的部分称为隐藏层（Hidden Layer），中间层输出的结果为激活值（Activation）
作为下一层的输入。

回顾之前学习逻辑回归和线性回归时，我们通过矩阵或向量的方式对模型计算和表达式进行了简化。同理，在神经网络模型中也可以通过使用向量表示输入特征值和激活值的方式进行简化。

![](https://i1.hdslb.com/bfs/note/38992e9fcdd7c7a0c2c5629057ec09021c32f968.jpg@690w_!web-note.avif)

## 另一种看待神经网络的角度

如果我们遮住左侧的输入层，可以发现右侧剩余的部分其实就是一个单纯的逻辑回归，只是这个逻辑回归并没有使用最原始的特征值，而是神经网络经过学习后自动选出的最好的其他特征，也
就是这些中间特征并不需要我们手工设置，而是由神经网络自动选出。

## 有多个隐藏层的神经网络

除了在上述示例中展示的只包含一个隐藏层的神经网络外，还存在一些神经网络是包含多个隐藏层的，通常按照顺序称为第一隐藏层、第二隐藏层等，这些神经网络也会被称为多层感知器（Multilayer Perceptron）。

![](https://i1.hdslb.com/bfs/note/fd67f7608014e1714aae7f6ce3801e3326cf5df1.jpg@690w_!web-note.avif)

# 示例-图像识别

## 人脸识别问题解析

我们再来讲解一个实际中的例子：图像识别。对于 1000 像素乘 1000 像素的人像图片，每个像素块都可以用一个值（0 至 255）来标识高度的高低，然后将这些值转换为向量进行表示，就是
一个拥有 100 万个元素的向量。

![](https://i1.hdslb.com/bfs/note/3521751e22292f02389c01466687e6db6e72615b.jpg@690w_!web-note.avif)

## 不同隐藏层会自动寻找不同特征

正如我们前面提到的，神经网络能够自己在不同的隐藏层学习这些特征检测器。在人像识别的这个示例中，神经网络可能会在第一隐藏层检测一些边缘和轮廓；第二层寻找一些人脸五官；第三层对
比整个五官的组合。

![](https://i1.hdslb.com/bfs/note/b25adaa0694d643b12b9b1ffd80ab06f51e1fdcc.jpg@690w_!web-note.avif)

## 使用不同数据集训练相同的神经网络

如果我们使用其他的数据集对相同的神经网络进行训练，它也会自动学习检测不同的特征，然后尝试输出预测结果。

![](https://i1.hdslb.com/bfs/note/a6e076b0d47e48e36cc21ac4620c431816acbbf2.jpg@690w_!web-note.avif)

# 神经网络中的层

## 拆解第一个隐藏层中的计算

对于第一层，其实也可以看作是一个逻辑回归，输入是向量 x，层数我们通过上标的 `[i]` 表示，而下标 j 表示是当前层的第 j 个神经元参数。同理，我们也可以用同样的方式拆解第二层，与第
一层不同的是，它的输入是上一层隐藏层的计算结果，可以称为向量 a。而最后的输出结果是标量，它其实就是我们之前在逻辑回归中用于判断的阈值。当这个计算结果大于等于阈值 0.5 时，我们
认为 y 的结果为 1（或者认为 y 为 1 的机率为较高）；相反当计算结果小于阈值 0.5 时，我们可以认为 y 的结果为 0（或者认为 y 为 0 的机率较高）。

![](https://i1.hdslb.com/bfs/note/a456839bb1d795aa3db1432406a615926ba07e54.jpg@690w_!web-note.avif)
![](https://i1.hdslb.com/bfs/note/98d57c17db3d74795913b416e86da5b19aff90e1.jpg@690w_!web-note.avif)

# 更复杂的神经网络

## 考察相关参数上标和下标问题

我们可以考察更加复杂的神经网络。

![](https://i1.hdslb.com/bfs/note/f5a23cec16f667f221d7e38b7fee93f159f7439d.jpg@690w_!web-note.avif)

## 适用于任意层的激活值计算公式

如果将最初的输入层视为第零层，我们就可以使公式适用于所有层。

![](https://i1.hdslb.com/bfs/note/f29f88db8b549187e2593f01e06ef1ac190d700e.jpg@690w_!web-note.avif)

# 做出预测（前向传播）

在目前我们讲到的神经网络示例中，都是通过从左向右的顺序，将输入数据转化为最终输出预测的步骤，这种计算过程称为前向传播（Forward Propagation）。

## 手写数字的识别

接下来通过手写数字识别的示例讲解前向传播。

![](https://i1.hdslb.com/bfs/note/526b37951fac81eee356b476298d00b06f58634c.jpg@690w_!web-note.avif)

# 代码中的推理

在实际情况下通过编写代码实现神经网络（使用 TensorFlow）分析具体问题，详情参考 Jupyter Lab。

## 示例-烘焙咖啡豆

在实际生活中，有些人喜欢自己烘焙咖啡豆，在烘焙过程中，咖啡豆的成熟主要受到两个因素的影响，一个是烘焙时间，另一个是烘焙的温度。在神经网络的实际示例中，我们就可以通过输入烘焙的温度和时间，然后通过机器学习预测是否烘焙到位。

[TODO: 绘制咖啡烘焙的散点图]

代码示例查看对应的 jupyter lab 文件。

